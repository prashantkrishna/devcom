<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Developers Community", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">

        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "My Profile", member_path(current_user), class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "My Network", connections_path, class: "nav-link active" %>
        </li>
        <li class="nav-item">
        <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: 'Are you sure to signed out?'}, class: "nav-link active" %>
        </li>
        <% else %>
        <li class="nav-item">
          <%= link_to "Sign In",  new_user_session_path, class: "nav-link active" %>
        </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>
